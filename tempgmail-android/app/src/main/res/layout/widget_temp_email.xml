<?xml version="1.0" encoding="utf-8"?>
<!-- Разметка виджета рабочего стола: последний адрес + кнопки -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@android:color/white"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_no_email"
        android:textSize="14sp"
        android:ellipsize="middle"
        android:singleLine="true"
        android:textColor="@android:color/black" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">

        <Button
            android:id="@+id/widget_btn_generate"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/widget_generate" />

        <Button
            android:id="@+id/widget_btn_copy"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/widget_copy" />
    </LinearLayout>
</LinearLayout>
