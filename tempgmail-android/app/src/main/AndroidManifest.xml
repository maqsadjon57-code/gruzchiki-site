<?xml version="1.0" encoding="utf-8"?>
<!-- Манифест приложения «Временный Gmail».
     Здесь регистрируются Activity, виджет, провайдер файлов и разрешения. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Сеть: все запросы только по HTTPS (Gmail API) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Уведомления о новых письмах (Android 13+) — запрашивается в рантайме -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TempGmailApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.TempGmail"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="false"
        android:supportsRtl="true">

        <!-- Единственная Activity: весь UI на Jetpack Compose -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Открытие из виджета по «сгенерировать» -->
            <intent-filter>
                <action android:name="com.tempgmail.app.action.GENERATE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Виджет рабочего стола: последний адрес + кнопка генерации -->
        <receiver
            android:name=".widget.TempEmailWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/temp_email_widget_info" />
        </receiver>

        <!-- FileProvider для безопасного шаринга CSV/JSON экспорта -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.tempgmail.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
